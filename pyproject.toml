[project]
name = "dropforge"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "psycopg[binary]>=3.3.6",
    "uvicorn>=0.53.0",
]

[dependency-groups]
dev = [
    "alembic>=1.20.0",
]
